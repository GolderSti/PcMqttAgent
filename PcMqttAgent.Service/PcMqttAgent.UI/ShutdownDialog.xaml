<Window x:Class="PcMqttAgent.UI.ShutdownDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Подтверждение действия" 
        Height="200" Width="421"
        WindowStartupLocation="CenterScreen"
        Topmost="True"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TxtMessage" 
                   Text="Запрос на выключение компьютера через MQTT." 
                   FontSize="16" 
                   TextWrapping="Wrap" 
                   Margin="0,0,0,10"/>

        <TextBlock x:Name="TxtTimer" 
                   Grid.Row="1"
                   Text="Автоматическая перезагрузка через: 15 сек." 
                   FontSize="14" 
                   Foreground="Red"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="BtnCancel" Content="Отмена" Width="100" Height="30" Margin="0,0,10,0" Click="BtnCancel_Click"/>
            <Button x:Name="BtnConfirm" Content="Подтвердить" Width="100" Height="30" IsDefault="True" Click="BtnConfirm_Click"/>
        </StackPanel>
    </Grid>
</Window>